# House Price Prediction

A regression model on the California Housing dataset — predicting median house values with machine learning.

## Setup
## Structure
- `notebooks/` — exploration and model development
- `src/` — reusable code
- `data/` — data files
